import { FormatedDatePipe } from './formated-date.pipe';

describe('FormatedDatePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatedDatePipe();
    expect(pipe).toBeTruthy();
  });
});
